const getters = {
    safetyAlarmList: (state) => state.data.safetyAlarmList,
    deviceAlarmList: (state) => state.data.deviceAlarmList,
    healthAlarmList: (state) => state.data.healthAlarmList,
};

export default getters;
